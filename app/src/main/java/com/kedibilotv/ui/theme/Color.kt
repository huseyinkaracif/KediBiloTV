package com.kedibilotv.ui.theme

import androidx.compose.ui.graphics.Color

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// Neon Gatos Cinema palette
// Film noir karanlığı  ×  neon arcade enerjisi
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

val NeonBackground   = Color(0xFF040E0E)   // Ultra koyu teal-siyah — sinema salonu
val NeonSurface      = Color(0xFF0A1A1A)   // Kart arka planı
val NeonSurfaceHigh  = Color(0xFF112020)   // Hover / seçili durum
val NeonSurfaceRim   = Color(0xFF1C3333)   // Kenar, ayırıcı

val NeonCoral        = Color(0xFFFF4500)   // Primary — cesur coral-turuncu
val NeonCoralLight   = Color(0xFFFF6B35)   // Primary varyant / hover
val NeonCyan         = Color(0xFF00FFD1)   // Accent — elektrik camgöbeği (sürpriz pop)
val NeonCyanDim      = Color(0xFF00C9A4)   // Accent dim
val NeonFuchsia      = Color(0xFFFF0080)   // Secondary — favoriler, kalpler
val NeonFuchsiaDim   = Color(0xFFCC0066)   // Secondary dim

val NeonTextPrimary  = Color(0xFFF0F8F8)   // Hafif teal ton beyaz
val NeonTextSecondary= Color(0xFF7AACAC)   // İkincil metin
val NeonTextMuted    = Color(0xFF3D6868)   // Soluk / disabled metin

val NeonError        = Color(0xFFFF3366)   // Hata
